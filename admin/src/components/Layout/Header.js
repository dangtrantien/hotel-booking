import styles from './Header.module.css';

// ==================================================

const Header = () => {
  return (
    <div className={styles.container}>
      <h1>Admin Page</h1>
    </div>
  );
};

export default Header;
